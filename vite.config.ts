// vite.config.ts
import electronViteConfig from './electron.vite.config'

// The object exported by electron‑vite is already a Vite config,
// so we can just export it directly.
export default electronViteConfig
